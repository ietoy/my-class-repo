const songs = document.querySelectorAll("#songs > li");



// console.log(songs);

for (const song of songs) {

    // console.log(song)

    // value.addClass(".red");

    song.classList.add("red");

}