const Child = require("../child");

describe("Child", () => {
    // positive test
    it("should return the child's infomration if they have a name and are under 6", () => {
        // arrange
        const newKid = new Child();

        // act
        // assert
        expect(newKid)toEqual();
    });
    // negative test:: negative age

    // exception test:
});
